/*
 * GensetPage Messages
 *
 * This contains all the text for the GensetPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GensetPage';

export default defineMessages({
  genset: {
    id: `${scope}.genset`,
    defaultMessage: 'Genset',
  },
});
