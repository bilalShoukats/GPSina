/*
 * DashboardScreen Messages
 *
 * This contains all the text for the DashboardScreen container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DashboardScreen';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardScreen container!',
  },
});
