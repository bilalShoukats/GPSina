// import { selectDriverDetailPageDomain } from '../selectors';

describe('selectDriverDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
