/*
 * ExpiredDevicePage Messages
 *
 * This contains all the text for the ExpiredDevicePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExpiredDevicePage';

export default defineMessages({
  expiredDevices: {
    id: `${scope}.expiredDevices`,
    defaultMessage: 'Expired Devices',
  },
});
