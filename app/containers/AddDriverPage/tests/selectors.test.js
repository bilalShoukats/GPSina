// import { selectAddDriverPageDomain } from '../selectors';

describe('selectAddDriverPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
