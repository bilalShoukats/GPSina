// import { selectZoneDetailPageDomain } from '../selectors';

describe('selectZoneDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
