// import { selectAddGensetPageDomain } from '../selectors';

describe('selectAddGensetPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
