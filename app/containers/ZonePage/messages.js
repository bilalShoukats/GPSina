/*
 * ZonePage Messages
 *
 * This contains all the text for the ZonePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ZonePage';

export default defineMessages({
  zone: {
    id: `${scope}.zone`,
    defaultMessage: 'Zone',
  },
  name: {
    id: `${scope}.name`,
    defaultMessage: 'name',
  },
});
