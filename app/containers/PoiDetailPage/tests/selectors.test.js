// import { selectPoiDetailPageDomain } from '../selectors';

describe('selectPoiDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
