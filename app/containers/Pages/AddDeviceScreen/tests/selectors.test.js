// import { selectAddCompanyScreenDomain } from '../selectors';

describe('selectAddCompanyScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
