// import { selectAddUserScreenDomain } from '../selectors';

describe('selectAddUserScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
