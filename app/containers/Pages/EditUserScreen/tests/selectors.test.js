// import { selectViewUsersScreenDomain } from '../selectors';

describe('selectViewUsersScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
