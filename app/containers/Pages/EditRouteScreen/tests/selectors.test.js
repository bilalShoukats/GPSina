// import { selectEditRouteScreenDomain } from '../selectors';

describe('selectEditRouteScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
