// import { selectGensetDetailPageDomain } from '../selectors';

describe('selectGensetDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
