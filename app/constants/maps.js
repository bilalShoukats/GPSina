const height = window.innerHeight - 55 + 'px';
const width = window.innerWidth + 'px';
const ASPECT_RATIO = width / height;
const LATITUDE = 3.095009;
const LONGITUDE = 101.6746841;

export {
    // app dimensions
    width,
    height,
    ASPECT_RATIO,

    // default region value on maps
    LATITUDE,
    LONGITUDE,
};