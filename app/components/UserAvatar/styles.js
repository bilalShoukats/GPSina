import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(theme => ({
  avatar: {
    width: '70px',
    height: '70px',
    justifyContent: 'center',
    alignItems: 'center',
  },
}));

export { useStyles };
